name: 🍎 iOS Build & TestFlight Deploy

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]
  workflow_dispatch:

env:
  EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
  EAS_PROJECT_ID: ${{ secrets.EAS_PROJECT_ID }}

jobs:
  # iOS Build and TestFlight Upload
  ios-build-and-deploy:
    name: 🚀 Build iOS & Deploy to TestFlight
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/dev' || github.event_name == 'workflow_dispatch'
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18.x
          cache: npm

      - name: 🏗 Setup EAS CLI
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🍎 Build iOS for TestFlight
        id: ios_build
        run: |
          echo "🔨 Building iOS app for TestFlight submission..."
          echo "⏰ Started at: $(date)"
          eas build --platform ios --profile preview --non-interactive --wait
          echo "✅ Build completed at: $(date)"

      - name: 🚀 Submit to TestFlight
        id: ios_submit
        run: |
          echo "🚀 Submitting to TestFlight..."
          eas submit --platform ios --profile preview --latest --non-interactive
          echo "✅ Submitted at: $(date)"

      - name: 🎉 Success Summary
        if: success()
        run: |
          echo "## 🎉 iOS Build & TestFlight Success! 🚀" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ✅ What Just Happened:" >> $GITHUB_STEP_SUMMARY
          echo "- 🔨 **iOS app built successfully** using EAS Build" >> $GITHUB_STEP_SUMMARY
          echo "- 🚀 **Automatically uploaded to TestFlight**" >> $GITHUB_STEP_SUMMARY
          echo "- 📱 **Testers will receive notifications** shortly" >> $GITHUB_STEP_SUMMARY
          echo "- ⚡ **No manual intervention needed**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📱 For Your Testers:" >> $GITHUB_STEP_SUMMARY
          echo "- Check the **TestFlight app** for the new build" >> $GITHUB_STEP_SUMMARY
          echo "- New build will be available for **internal testing immediately**" >> $GITHUB_STEP_SUMMARY
          echo "- Push notifications should arrive **within 5-10 minutes**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 Build Info:" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Build completed:** $(date)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🎯 **Next deployment:** Push to \`dev\` branch again anytime!" >> $GITHUB_STEP_SUMMARY

      - name: 📢 Slack Success Notification
        if: success()
        uses: 8398a7/action-slack@v3
        with:
          status: custom
          webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
          custom_payload: |
            {
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "🎉 iOS TestFlight Deploy Success!"
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*App:* Aji Salit"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Platform:* 🍎 iOS"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Status:* ✅ Success"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Deployed by:* ${{ github.actor }}"
                    }
                  ]
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*Branch:* `${{ github.ref_name }}`"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Commit:* `${{ github.sha }}`"
                    }
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "🚀 *New build uploaded to TestFlight!*\n📱 Testers will receive notifications shortly\n⏰ Build completed at: $(date)"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "View Build Logs"
                      },
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    },
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "Open TestFlight"
                      },
                      "url": "https://appstoreconnect.apple.com/apps/6744866152/testflight/ios"
                    }
                  ]
                }
              ]
            }

      - name: ❌ Failure Summary
        if: failure()
        run: |
          echo "## ❌ iOS Build Failed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔍 Troubleshooting Steps:" >> $GITHUB_STEP_SUMMARY
          echo "1. **Check the build logs** above for specific error messages" >> $GITHUB_STEP_SUMMARY
          echo "2. **Verify credentials** - run \`eas build --platform ios --profile preview --auto-submit\` locally" >> $GITHUB_STEP_SUMMARY
          echo "3. **Check App Store Connect** - ensure your app and certificates are active" >> $GITHUB_STEP_SUMMARY
          echo "4. **Verify bundle identifier** matches what's configured in App Store Connect" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔧 Quick Fixes:" >> $GITHUB_STEP_SUMMARY
          echo "- If credentials issue: Run \`eas credentials -p ios\` locally" >> $GITHUB_STEP_SUMMARY
          echo "- If build issue: Check \`app.json\` and \`eas.json\` configuration" >> $GITHUB_STEP_SUMMARY
          echo "- If submission issue: Check App Store Connect API key status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Failed at:** $(date)" >> $GITHUB_STEP_SUMMARY

      - name: 📢 Slack Failure Notification
        if: failure()
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "❌ iOS Build Failed"
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*App:* Aji Salit"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Platform:* 🍎 iOS"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Status:* ❌ Failed"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Triggered by:* ${{ github.actor }}"
                    }
                  ]
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*Branch:* `${{ github.ref_name }}`"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Commit:* `${{ github.sha }}`"
                    }
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "🔍 *Build failed during deployment*\nCheck the logs for details and troubleshoot the issue."
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "View Error Logs"
                      },
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                }
              ]
            }' \
            ${{ secrets.SLACK_WEBHOOK_URL }}
